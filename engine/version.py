"""Версия методики измерения движка (Фаза 3, Компонент 4).

`profile_store` складывает уже посчитанные агрегаты — сырых сэмплов там нет, и
пересчитать историю нельзя (нужен повторный прогон YOLO по видео, которого может
уже не быть). Фаза 3 сменила определения `mae_hu`/`std_hu` (атрибуция цели) и
`placement.below` (гейт пре-айма): старые записи остались по прежней методике.

Чтобы агрегация и петля прогресса Фазы 2B не смешивали методики (иначе смена
методики отрапортуется как «прогресс» игрока), каждая запись/отчёт штампуется
`METRICS_VERSION`; записи без поля считаются версией 1 и в сравнение не входят.

Инкрементировать при ЛЮБОМ изменении определения числа, которое накапливается
в профиль или сравнивается через историю.
"""

# v1 — методика до Фазы 3 (записи без поля). v2 — атрибуция цели + гейт пре-айма.
METRICS_VERSION = 2
